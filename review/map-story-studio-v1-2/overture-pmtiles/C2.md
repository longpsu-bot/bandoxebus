# Overture PMTiles C2 — Route 61-2 certification

**Result: PARTIAL — HOSTED R2 GATE PENDING.** All available functional gates passed. Physical-GPU performance is **NOT_MEASURED**; the software-rendered performance measurements below remain diagnostics and do not certify the physical-performance threshold.

Measured on 2026-09-03 in [Actions run 33763268580](https://github.com/longpsu-bot/bandoxebus/actions/runs/33763268580), attempt 1, using exact implementation head [`f7e7181cb4b8e98e50b8658a91a7a103a7b98789`](https://github.com/longpsu-bot/bandoxebus/commit/f7e7181cb4b8e98e50b8658a91a7a103a7b98789). The credential-free browser job and ordinary test job both succeeded. Certification ran from `13:49:28.266Z` to `13:50:39.569Z`.

The run artifact is `overture-pmtiles-c2-f7e7181cb4b8e98e50b8658a91a7a103a7b98789-1`, with evidence under `run-85VYJ1/`: `result.json`, `freeze-plan.json.capture.json`, `freeze-1.json`, `freeze-2.json`, unchanged-C1 logs, `c1-functional.json`, `same-runner-official-diagnostic.json`, `c2-browser.json`, and `c2-browser.png`. Actions artifacts have a 14-day retention period; the measured values are retained here. Generated archives, native executables, and temporary projects are not committed.

## Identity and regression tests

| Check | Measured result |
| --- | --- |
| Implementation head | `f7e7181cb4b8e98e50b8658a91a7a103a7b98789` |
| Canonical main and merge base | `88e6c4c88088b8170d8c592aab004e275b1b8fc2` |
| Canonical base tree | `427ca7dc07b3889a8d250d013e21ff73c53ab684` |
| Main re-fetch immediately before extraction | Same canonical base; checked `13:49:45.170Z` |
| Five protected files versus canonical main | **EMPTY DIFF** |
| Canonical Story CRLF SHA-256 | `29597ee58773b13ff9db6eaf3c328240f6bfa85f9bf7161cdca7b20ad55b373a` |
| Linux raw Story SHA-256, 8,935 bytes | `5f374a8901866c23e473107f4279f929304ec0645c170d3340ad028ac89267a9` |
| Final local focused set | **260/260 PASS**, 21,025.8913 ms |
| Single final local full suite | **838/838 PASS**, 53,660.4328 ms; zero failures/skips/cancellations |
| CI full suite on implementation head | **838/838 PASS**, 5,139.355301 ms |

The protected files are both Story schemas, `data/stories/route-61-2.story.json`, `src/map/geojson-renderer.js`, and the retained 1,299-building `data/context/my-phuoc-1-buildings.geojson`. The original C1 harness and `C1.md` also have an empty diff from the base. Linux checkout bytes were copied unchanged; the canonical CRLF digest was computed without modifying files. The local Windows Story digest independently matched the canonical CRLF value.

Final local verification used Node `24.18.0`. The focused command was:

```text
node --test tests/project-schema.test.mjs tests/capability-descriptors.test.mjs tests/project-loader.test.mjs tests/overture-pmtiles.test.mjs tests/urban-context.test.mjs tests/route-61-2-runtime-adapter.test.mjs tests/editor-folder-storage.test.mjs tests/editor-zip-storage.test.mjs tests/editor-package-resolver.test.mjs tests/editor-preview-bridge.test.mjs tests/editor-studio-preview.test.mjs tests/editor-capability-authoring.test.mjs tests/overture-freeze-plan.test.mjs tests/overture-freeze-tool.test.mjs tests/overture-freeze-project.test.mjs tests/hosted-pmtiles-resolver.test.mjs tests/route-61-2-project.test.mjs tests/application-composition.test.mjs tests/overture-pmtiles-c2-certification.test.mjs
```

It was followed once locally by `npm.cmd test` (the Windows invocation of `npm test`, whose package script is `node --test`). CI used Node `24.20.0`.

## Production Prepare Freeze

Studio opened the saved eight-file authoring package as `Valid production project · revision 0`. Prepare Freeze captured the actual production map, canvas, and viewport at both specified dimensions; all three measurements matched each profile. The only context-active Scene was `service-area`, zero-based index 4 (Scene 5 of 7).

Project fingerprint:

```text
d01710a7f51a4e73030295e427bb2c64b9d7dfddeeb465342429cb31fa112484
```

All bounds below use `[west, south, east, north]`.

| Bounds | Measured value |
| --- | --- |
| Desktop Scene, 1920 × 1080 | `[106.48735121814451, 11.089294177449034, 106.67409895903631, 11.229274406955454]` |
| Mobile Scene, 390 × 844 | `[106.56504562124115, 11.10182038237754, 106.60811461338977, 11.198118700976565]` |
| Required project union | `[106.48735121814451, 11.089294177449034, 106.67409895903631, 11.229274406955454]` |
| Final project bounds, no enlargement | `[106.48735121814451, 11.089294177449034, 106.67409895903631, 11.229274406955454]` |
| Native archive header, E7 precision | `[106.4873512, 11.0892941, 106.6740989, 11.2292744]` |

Both captured cameras used center `[106.59576774999994, 11.129430096975923]`, zoom `13.6`, pitch `51.99999999999999`, and bearing `-10`. The native header is within the required absolute `1e-7` degree tolerance. These are measured project values, not production-code constants.

## Regional extraction correction and two clean Freezes

The original native extraction inherited global MinZoom 0, although the regional result began at z9; native `pmtiles verify` correctly rejected the inconsistent header. Pinned go-pmtiles supports an explicit extraction minimum and writes that requested minimum into its output header ([v1.31.2 CLI options](https://github.com/protomaps/go-pmtiles/blob/v1.31.2/main.go), [extractor implementation](https://github.com/protomaps/go-pmtiles/blob/v1.31.2/pmtiles/extract.go)).

The Task 9 ruling is implemented with supported `--minzoom=11`, the existing C1 flat-layer floor, in both dry run and extraction. Maximum zoom is inherited and checked for exact equality with the source. The result preserves official tile payloads and source metadata, remains one project extraction, and still must pass native verification. Tiles below z11 are excluded; any future renderer requiring those zooms would need a revised extraction floor. No custom writer, tiler, or archive-header patch was introduced.

| Extraction evidence | Measured result |
| --- | --- |
| Input | Official pinned Overture Buildings release `2026-08-19.0` |
| `sourceKind` / retained-archive SHA | `official` / `null` (retained-local input was not used) |
| Native tool | `pmtiles 1.31.2`, commit `a3e4951ea6a0477b784c27c1dcbfd9c130878c5a`, built `2026-07-22T18:59:03Z` |
| Verified Linux release artifact | `go-pmtiles_1.31.2_Linux_x86_64.tar.gz` |
| Release artifact SHA-256 | `3ed7dbf4ec2e6dfe5e25b6f70d1ffc932729f93c86db353bf514dd71010a312f` |
| Fixed options | `--minzoom=11 --download-threads=4 --overfetch=0.05`; one final project bbox; source maximum inherited |
| Dry-run predicted archive size | `8.1 MB`, parsed as `8,100,000` bytes; upstream display is rounded |
| Actual snapshot size, both runs | **8,118,241 bytes**, below 32 MiB and the unchanged 64 MiB ceiling |
| Header | Clustered MVT; gzip tile/internal compression; minimum `11`, maximum `14` |
| Addressed tiles / entries / contents | `110 / 110 / 110` |
| Source maximum | `14`, preserved exactly |
| Source metadata | Deep-equal to extracted metadata, including `building` source-layer |
| Source ETag | `"b6c742a2a3104d82933885eb5062707c-672"` |
| Source content length | `180,364,329,147` bytes |
| Freeze 1 | **PASS**, CLI exit `0`, `13:49:45.171Z`–`13:49:48.523Z` |
| Freeze 2, distinct clean output | **PASS**, CLI exit `0`, `13:49:48.524Z`–`13:49:50.587Z` |
| Native `pmtiles verify`, each Freeze | **PASS**, mandatory before publication construction |
| Frozen production validation, each Freeze | **PASS**, mandatory before atomic output publication |
| Authoring inventory before/after | Exact byte-hash equality for all eight managed files |
| Duplicate comparison | Byte/hash equality; verified duplicate removed at `13:49:51.389Z`; first output retained |

Snapshot SHA-256, **Freeze 1 = Freeze 2 = Folder = ZIP import**:

```text
3488b46ce990c72129e33abddee319d5afa41ad11d58f2fe167ed04ef33ec605
```

A separate pinned Windows 1.31.2 extraction of the same official source and captured bbox also produced exactly 8,118,241 bytes with this SHA and passed native `verify`. The downloaded Linux artifact was independently checked with the pinned Windows CLI and passed `verify`; its header supplied the E7 bounds above. This establishes Windows/Linux byte identity for the measured source, options, and project extent. Publication timestamps stay in manifests rather than in PMTiles bytes.

## Frozen Folder, Save, and ZIP

The browser used native disk-backed `File` objects supplied through CDP and a test `DirectoryHandle` facade. It exercised the real Folder adapter, production preview, Save, and ZIP import/export paths. Native picker and permission UX were not measured.

| Gate | Measured result |
| --- | --- |
| Frozen Folder Open | Nine managed files; valid production project |
| Full PMTiles `arrayBuffer()` calls at Folder Open | `0` |
| Before first activation, including context-off navigation | Full reads `0`; slices `0`; protocols `0`; sources `0` |
| First activation | One upstream FileSource; `13` slices, `243,603` bytes; full reads `0` |
| Rendered frozen features at first activation | `4,619` |
| Unrelated Save | Only `project.json` written; failures `[]`; skipped `[]` |
| Snapshot SHA before/after Save | Both equal the full snapshot SHA above |
| Snapshot mtime before/after Save | Both `1788443419659.842` ms since epoch |
| Folder → ZIP size | `8,132,019` bytes |
| Imported PMTiles size and SHA | `8,118,241` bytes; exact snapshot SHA above |
| Imported production validation | Exactly `Valid` |
| Imported frozen preview | `4,619` rendered building features |
| Official Overture requests throughout frozen certification | **0**, including after ZIP import |

## C2 camera and source stability

The production preview and canvas both measured `1440 × 900`. Its starting camera was center `[106.59576774999994, 11.129430096975923]`, zoom `13.6`, pitch approximately `52°`, bearing `-10°`; buildings at this zoom use the flat layer. The harness counts rendered features from the applicable flat/extrusion layers.

Completed bearing `0°` through `360°` in `30°` steps, pitch `0° → 52° → 0°`, and a bounded pan away and back. Scene A → B → A navigation completed with source reuse. Scene B has context off and bounds `[106.4442091466554, 10.945408188879668, 106.78330479225218, 11.206391739677997]`, outside the frozen extent; two context-active in-bounds Scene cameras are **NOT_APPLICABLE_OUTSIDE_SNAPSHOT** for this canonical Story. No dual-location frozen coverage is claimed.

| Runtime measurement | Before sweeps | After sweeps and Scene return |
| --- | ---: | ---: |
| MapLibre maps | 1 | 1 |
| Canvases | 1 | 1 |
| PMTiles protocol registrations | 1 | 1 |
| FileSource constructions | 1 | 1 |
| Building sources | 1 | 1 |
| Flat layers | 1 | 1 |
| Extrusion layers | 1 | 1 |
| Source additions | 1 | 1 |
| Flat-layer additions | 1 | 1 |
| Extrusion-layer additions | 1 | 1 |
| Source removals | 0 | 0 |
| Layer removals | 0 | 0 |

Cumulative FileSource reads after the sequence: **49 slices, 910,608 bytes, 0 full reads**. Beyond first activation, the sequence added 36 slices and 667,005 bytes. Unexpected map/runtime errors: **0**. These are aggregate sequence counts, not separate per-sweep byte measurements.

## C1 functional regression and software diagnostics

The **unchanged** C1 harness was rerun on the exact implementation head and exited `1` at its original 250 ms activation threshold: worst frame gap `316.6999999999998` ms, worst long task `266` ms. It stopped before its final FPS result and later functional assertions; that exit is retained as a failed timing result, not relabeled a full C1 PASS.

The independent C1 functional replay then executed the remaining behavior with blocking assertions and recorded **PASS**:

| C1 functional measurement | Result |
| --- | --- |
| Official requests before activation | `0`; status `not-requested`; source/layers absent; protocol additions `0` |
| Mỹ Phước | `12` archive requests; Range/206 observed; `14,006` rendered and `31,170` source features |
| Thủ Dầu Một | `7` new archive requests after the move; Range/206 observed; `23,681` rendered and `60,024` source features |
| Distant camera arrival | `0` metres from `[106.65, 10.98]`; zoom `15`, pitch `48`, bearing `-12` |
| First activation and warm reuse | One map/canvas/protocol/source/flat/extrusion set; source/flat/extrusion additions each `1`; source/layer removals `0` |
| Injected official-source failure | Status `unavailable`; route retained; Story advances to Scene 6; source stays vector; no synthetic fallback |
| Unexpected console/map errors | `0` |
| Instrumentation cleanup | `removed` |

One expected network error was attributed to the deliberately failed official archive request: request ID `2280.1032`, Fetch request ID `interception-job-540.0`, `Network.loadingFailed` text `net::ERR_FAILED`. The console observation had the same URL and request ID in the `remote-failure` phase and was matched once to that completed injection. Unmatched, pre-injection, wrong-ID, unrelated-URL, and runtime errors remain blocking.

The same-viewport official-source diagnostic independently recorded 15 archive responses, all HTTP `206`; its initial `Range: bytes=0-16383` received `Content-Range: bytes 0-16383/180364329147`. This diagnostic used the same `1440 × 900`, z13.6 production camera as C2.

Renderer: `ANGLE (Google, Vulkan 1.3.0 (SwiftShader Device (Subzero) (0x0000C0DE)), SwiftShader driver)` on the Ubuntu Actions runner.

| Software measurement | Official-source diagnostic | C2 frozen FileSource |
| --- | ---: | ---: |
| Settled FPS | 56.71455340412243 | 50.91980131293191 |
| Frames / elapsed milliseconds | 227 / 4002.5 | 204 / 4006.3000000000175 |
| Worst activation frame gap, ms | 299.9000000000001 | 366.6999999999998 |
| Worst activation long task, ms | 203 | 334 |
| Worst activation frame/task gap, ms | 299.9000000000001 | 366.6999999999998 |

Same-runner, same-viewport C2/official FPS ratio: **0.8978260121366077**. The unchanged C1 harness did not emit a final FPS value after its timing failure, so no ratio against that missing value is reported. Activation timing includes production camera and basemap work, not isolated PMTiles CPU time.

C2's software-performance assessment remains **FAIL** against the existing 54 FPS / 250 ms diagnostic checks. Neither that result nor the relative ratio is a physical-GPU certification. Physical-GPU status: **NOT_MEASURED**. Existing C1 hardware evidence is unchanged.

## External completion gates

- Hosted R2: **PENDING_EXTERNAL_ENDPOINT**. No deployment-owned public snapshot URL was supplied; no bucket was created and no snapshot was uploaded. No real R2 Range/206 probe or hosted browser PASS is claimed. The deployment hook remains empty, with no environment-specific URL committed.
- Physical GPU: **NOT_MEASURED**. Actual hardware-accelerated evidence is still required for physical-performance certification.

PR #37 remains Draft and unmerged. Pages serves the application preview; it is not the frozen production PMTiles host. The implementation retains direct extraction, one project snapshot, FileSource for Folder/ZIP, and the bounded R2 FetchSource deployment path, with no per-Scene extraction, tile server, proxy, Worker, service worker, custom tile cache, custom tiler, or fallback provider.
